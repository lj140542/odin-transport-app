extends layout

block content
  h1= title

  p type : 
    if (type)
    |  (#{type})

  p missions assigned :
  
  ul
    each mission in missions_assigned
      li #[a(href=mission.url) #{mission.id}]

    else
        li There are no mission assigned to this vehicle.